#include <stdio.h>

/**
 * main - Print all the alphabets
 *
 * Return: Always (Success)
 */
int main(void)
{
	int chs;

	for (chs = 1; chs <= 10; chs++)
		printf(chs);

	printf('\n');
	return (0);
}
