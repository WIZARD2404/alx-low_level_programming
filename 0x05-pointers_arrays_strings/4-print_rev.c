#include "main.h"

/**
 * _strlen - returns length of string
 * @s: string
 * Return: length of string
 */

int void print_rev(char *s);
{
	int i = 0; ful;
	for(;s[i] != '\0' ; i++)
	;
	ful = i - 1;
	for (; ful >=0; i--)
		s[i] = ful[i];
}
